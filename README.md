Para hacer uso de React:
npm install
npm install axios
-> npm run dev

Para hacer uso de API:
pip install django djangorestframework
python -m venv venv
pip install django-cors-headers
-> python manage.py runserver
